name: Elbphi Ticket Watch

on:
  workflow_dispatch:
  schedule:
    - cron: "7,17,27,37,47,57 * * * *"

jobs:
  watch:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # damit wir ticket_state.json zurück ins Repo committen können

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run watcher
        env:
          EVENT_URLS: |
            https://www.elbphilharmonie.de/de/programm/symphonieorchester-chor-des-bayerischen-rundfunks-sir-simon-rattle/23468
            https://www.elbphilharmonie.de/de/programm/royal-concertgebouw-orchestra-klaus-makela/23474
            https://www.elbphilharmonie.de/de/programm/strauss-elektra-alan-gilbert/23294
            https://www.elbphilharmonie.de/de/programm/konzerthausorchester-berlin-alice-sara-ott-joana-mallwitz/23142
          NTFY_URL: https://ntfy.sh
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: python watch.py

      - name: Commit state if changed
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "ticket-watcher"
            git config user.email "ticket-watcher@users.noreply.github.com"
            git add ticket_state.json
            git commit -m "Update ticket state"
            git push
          else
            echo "No changes."
          fi
